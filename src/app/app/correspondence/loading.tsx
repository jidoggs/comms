import React from 'react';
import FullPageLoader from '@/common/components/FullPageLoader';

const Loading: React.FunctionComponent = () => {
  return <FullPageLoader fullscreen />;
};

export default Loading;
